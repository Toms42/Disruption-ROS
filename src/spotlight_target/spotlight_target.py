#!/usr/bin/env python

# This node publishes the frames for each
#
# author Tom Scherlis

import rospy


class MocapBridge:
    def __init__(self):


def main():
    rospy.init_node('spotlight_target')
    mb = MocapBridge()
    mb.start()
