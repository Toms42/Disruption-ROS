robot_frames:
  - Snake
  - Frog
  - Bird

hat_frames:
  - hatA
  - hatB
  - hatC
  - hatD

time_per_robot_min: 8
time_per_robot_max: 15

# error threshold in degrees for the lamp to be on:
lamp_on_threshold: 10
lamp_state_topic: "/spotlight/azimuth"
lamp_target_topic: "/spotlight/azimuth_target"
lamp_on_topic: "/spotlight/bulb_state"

lamp_enable_topic: "/enable_hardware"
lamp_target_frame_topic: "/spotlight/target_frame_id"

hat_alpha: 0.1
teleport_threshold: 0.5

velocity_violation_threshold: 0.5
velocity_violation_persistence: 1